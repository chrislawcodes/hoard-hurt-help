# Plan

## Review Reconciliation

- review: reviews/spec.codex.feasibility-adversarial.review.md | status: accepted | note: Both MEDIUM findings accepted and fixed in the spec revision. (1) Mutable email: D8 rewritten to store users.is_internal at account creation instead of matching email live, plus an admin toggle to correct a wrong flag. (2) Distinct-user rule: added D9 requiring COUNT(DISTINCT users.id) everywhere, with a multi-agent regression test.
- review: reviews/spec.claude.requirements-adversarial.review.md | status: accepted | note: Round 2. 14 findings (5 HIGH), all verified before acceptance. Decisive: gc_pending_connections has no scheduler (only two page routes call it) so revision 2's 24-hour-snapshot tile was meaningless - tile dropped in D12. SessionMiddleware sets no max_age so the cookie is a persistent 14-day cookie, and the site has no privacy policy anywhere - now recorded as D8's OPEN OBLIGATION. No timestamp was named for played-a-turn; resolved by recording the milestone at write time (D5) and matching admin_reports' NULL-submitted_at guard. One reviewer claim was overstated and is recorded as such: revision 2 said anonymous visitors get no cookie at all, but google_login already writes a session - corrected in D8.
- review: reviews/spec.claude.completeness-adversarial.review.md | status: accepted | note: Round 2. 14 findings (3 HIGH), all verified before acceptance. Decisive: AgentKind has a third value HUMAN, and web_join._default_human_choice returns True for every brand-new user - manual play is the DEFAULT onboarding path, and under strict nesting a daily human player rendered as stuck at picked-a-handle. This finding is the single largest driver of the revision-3 redesign (D1/D2, AC4). Also fixed: tests/test_mcp.py's five two-parameter sync_google_user fakes added to scope; AdminAction now gains a paired unmark_internal; state.json constraints field corrected where it still asserted the withdrawn no-cookie non-goal; admin_user_actions.py and admin/dashboard.html added to the touch list; users_list colspan; dev@localhost seed domain; timezone default changed from UTC to browser.
- review: reviews/spec.gemini.requirements-adversarial.review.md | status: failed | note: Gemini CLI is non-functional on this machine: IneligibleTierError, Google ended Gemini Code Assist for individuals. Not a transient failure and retry cannot succeed. Substituted with spec.claude.requirements-adversarial (same lens, Claude reviewer, spec 020 path), which ran and is reconciled as accepted.
- review: reviews/spec.gemini.completeness-adversarial.review.md | status: failed | note: Gemini CLI is non-functional on this machine: IneligibleTierError, Google ended Gemini Code Assist for individuals. Not a transient failure and retry cannot succeed. Substituted with spec.claude.completeness-adversarial (same lens, Claude reviewer, spec 020 path), which ran and is reconciled as accepted.
- review: reviews/spec.claude.feasibility-adversarial.review.md | status: accepted | note: Round 2. 13 findings (4 HIGH), all verified against real code before acceptance. Decisive: seat_hold.py:87/:119 hard-deletes held players in ~15 min and agents_lifecycle.py:158 hard-deletes agents with no Player rows - both erase the exact abandonment cohorts the feature measures. Also: autopilot submissions are not marked defaulted so abandoners cleared played-a-turn; the .local internal accounts cannot have come through Google sign-in so a fourth creation path exists outside the app. These findings drove the revision-3 redesign to durable milestone records (D1) recorded at write time (D3/D5), which removes the read-time filtering these findings attacked.

# specs/

This folder is historical: each spec shows how a feature was planned, not how it works today.
A spec is not updated after its feature ships, so treat the architecture docs and the code as the source of truth for current behavior.
